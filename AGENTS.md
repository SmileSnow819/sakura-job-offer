# Sakura Job Offer

## 新对话开始

1. 先阅读 [项目背景与协作偏好](docs/ai/project-context.md)。它描述本项目的工作约定，不代表其他项目或用户的全部个人偏好。
2. 确认本次请求是讨论、诊断、审查还是实施；没有当前目标时先问，不从历史文档自动挑任务开做。
3. 用 `git status --short`、当前分支、近期提交和相关 diff 核对现场，再读本任务涉及的代码。未提交文件不一定属于本次任务，不擅自覆盖或提交。
4. 若是继续旧任务，读取用户指定的交接记录及关联设计/计划。交接记录是线索，以当前代码和本轮验证为准；不要假装能记住其他对话。
5. 简短说明理解的目标、改动范围和验证方式。已批准且未变更的方案不重复要求批准；有实质性范围变化再确认。

人如何发起任务、验收和交接，见 [AI 开发指南](docs/ai-development.md)。按任务阅读，避免每轮加载全部历史与 Skills。

## 项目内 Skills

- 开始开发任务时，先阅读 [Superpowers 入口](.agents/skills/superpowers/using-superpowers/SKILL.md)，再按任务加载需求讨论、计划、排错、测试或审查 Skill。文档中的 `superpowers:名称` 对应 `.agents/skills/superpowers/名称/SKILL.md`。
- 修改或评审 TypeScript、React、CSS 时，先阅读 [项目开发规范](.agents/skills/sakura-project-conventions/SKILL.md)。纯数据维护任务不要求加载前端规范。
- 审查 commit、分支、PR 或工作区 diff 时，再阅读 [项目 CR 规范](.agents/skills/sakura-code-review/SKILL.md)。先报告有证据的问题；未经用户要求不顺手修改。
- 涉及 Vite+、依赖、开发服务、格式化、检查、测试或构建时，阅读 [Vite+ 工作流](.agents/skills/vite-plus/SKILL.md)。

## 代码维护与复用

- 实现功能前先检查项目现有的组件、Hooks、工具函数、类型和样式；能够直接复用或在原有能力上合理扩展时，不要在不同位置重复实现相同逻辑。复用会造成职责混乱或过度耦合时再拆分，避免为了复用而做无意义的抽象。
- 新增或修改代码时，补充有助维护的中文注释，重点说明职责、业务原因、复杂流程、边界条件和约束；具体适用范围与例外以项目开发规范为准，避免给简单局部函数机械补注释。
- 导出函数、组件、Hooks 和包含业务规则的具名函数应使用 JSDoc；独立参数与非显然返回语义分别使用 `@param`、`@returns`。Props 解构组件不机械重复接口字段。
- 修改实现时同步检查并更新相关注释和 JSDoc，不保留与当前代码不一致的说明。

## 验证与改动边界

- 讨论、解释、诊断、审查默认只读；明确要求实施后才修改。Superpowers 的设计确认不能被“安装了 Skill”替代，也不能覆盖用户对当前任务的明确流程指示。
- 前端实现完成后运行 `pnpm check` 和受影响的测试；`pnpm check` 已包含格式、lint 和类型检查。生产构建由 `.githooks/pre-commit` 在每次提交前执行，日常实现完成时不重复运行；不提交时仅在发布、部署或用户明确要求完整验证时手动运行。纯文档修改检查格式、链接和命令一致性即可。交互、样式和导出改动还需要对应的实际验收，不能用构建通过代替。
- 没运行的验证明确标为未运行；代码已修改、测试通过、视觉确认、已提交、已推送是不同状态，分别汇报。
- `data/`、`src/bookmarks.json`、`src/mottos.json` 是业务数据，不混入代码格式化改动。
- 保留用户的未提交修改；提交、推送和部署只在用户要求时执行。提交按功能拆分；推送 `main` 会触发现有 Pages 工作流，不能把“提交一下”理解成推送。
- Superpowers 按当前环境实际可用的工具执行；安装 Skill 不代表授权修改全局配置、自动提交、删除用户代码或创建新任务。缺少多代理工具时使用单代理执行流程，不用创建聊天任务冒充子代理。
- 只把用户确认的持续性偏好写入项目背景；具体任务的进度与待办留在相应设计、计划或交接记录中，不在本入口堆积。
